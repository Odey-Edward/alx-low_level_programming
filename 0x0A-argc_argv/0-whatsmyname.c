#include <stdio.h>
#include <stdlib.h>

/**
* main - Entry point
* @agrc: argument count
* @agrv: argument vector
* Return: 0 always (success)
*/
int main(int agrc, char *agrv[])
{
	printf("%s\n", agrv[0]);

	return (0);
}
